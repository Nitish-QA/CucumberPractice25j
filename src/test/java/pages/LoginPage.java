package pages;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;

public class LoginPage {

	/*
	 * protected WebDriver driver;
	 * 
	 * private By txt_username_login = By.id("name"); // WebElement eleLogin;
	 * 
	 * private By txt_password_login = By.id("password");
	 * 
	 * private By btn_login = By.id("login");
	 * 
	 * private By txt_logout = By.id("logout");
	 * 
	 * public LoginPage(WebDriver driver) { this.driver = driver; }
	 * 
	 * public void enterUsername(String username) {
	 * driver.findElement(txt_username_login).sendKeys(username); }
	 * 
	 * public void enterPassword(String password) {
	 * driver.findElement(txt_password_login).sendKeys(password); }
	 * 
	 * public void clickLogin() { driver.findElement(btn_login).click(); } public
	 * void validate_login() { driver.findElement(txt_logout).isDisplayed(); }
	 * 
	 * 
	 * public void loginValidUser(String username , String password) {
	 * driver.findElement(txt_username_login).sendKeys(username);
	 * driver.findElement(txt_password_login).sendKeys(password);
	 * driver.findElement(btn_login).click(); }
	 * 
	 */
}
